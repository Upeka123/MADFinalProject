<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.expensetrackerforuniversitystudent">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseTrackerForUniversityStudent">
        <activity android:name=".budgetActivity" />
        <activity android:name=".MainActivityExpenses">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".SplashScreenActivity">

        </activity>

<!--        pasindu-->
        <activity android:name=".QandA"/>
        <activity android:name=".SubmitAnswers" />
        <activity android:name=".Answers" />
        <activity android:name=".EditQuestion" />
        <activity android:name=".AddQuestion"/>


    </application>

</manifest>